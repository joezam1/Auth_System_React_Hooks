const WindowLocationProperty = Object.freeze({
    PROTOCOL:'protocol',
    HOST:'host',
    PATHNAME:'pathname',
    SEARCH:'search',
    REFERRER:'referrer',
    REDIRECT:'redirect'

});

export default WindowLocationProperty;